%h2 Your dealt cards
.cards
  - @cards.each do |card|
    .card-container
      .card-header
        %h3
          %a{href: card_path(card)}= card.title
      %div{class: "card-body card-#{card.content_type.downcase}"}
        = render card.content
